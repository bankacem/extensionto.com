// WARNING: Storing passwords directly in client-side JavaScript is INSECURE and NOT recommended for production applications.
// This is for demonstration purposes only.
const config = {
    password: "0600231590" // This password is for demonstration purposes only and is not secure.
};

export default config;
